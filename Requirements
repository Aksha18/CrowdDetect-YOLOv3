opencv-python==4.7.0.72
numpy==1.23.4
matplotlib==3.7.1
pandas==1.5.3
opencv-python: Required for computer vision tasks (e.g., loading videos, detecting objects using YOLO).
numpy: A core dependency for working with arrays and matrices (e.g., in YOLO’s object detection).
matplotlib: For creating visualizations (e.g., displaying frames with bounding boxes).
pandas: Useful if you need to manage or analyze any data in your project.
